# Hybrid production UI — done when

The approved production hybrid is complete only when all of the following are true:

- The public surface uses the Patchwork Atlas landing direction, including the approved recovery promise, three recovery steps, privacy boundary, mandatory Neighborhood Patch mark/palette/seam motif, Telegram sign-in, and a working demo-video link.
- The authenticated surface uses the Threadline Welcome/Cases/Thread shell and visibly discloses compressed demo timing.
- The overview and case-detail surfaces use joined Mending Table workbenches, preserve evidence provenance, and keep every case-changing action in Telegram.
- No mock data, health scores, member archetypes, weekly briefings, or protected recovery content render before authorization.
- Session resume, logout, denial redaction, retryable failures, Telegram configuration retry, overview filtering, nested case routing, and route focus work without horizontal overflow at 1440×900 and 390×844.
- The real Telegram widget is permitted by the production CSP, the production build contains the selected logo and demo MP4, and the client uses the typed protected API.
- Type checks, all Vitest tests, the production build, all Playwright journeys, automated axe checks, the UI audit, and the repository phase-3 gate pass.

Verification commands:

```bash
npm run verify
npm run test:e2e
node /Users/mac/.agents/skills/ui-revamp/scripts/audit.js src/dashboard
./verify.sh phase-3
```
